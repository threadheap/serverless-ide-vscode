{
    "name": "serverless-ide",
    "private": true,
    "engines": {
        "vscode": "^1.26.0"
    },
    "devDependencies": {
        "@types/jest": "^23.3.12",
        "husky": "^1.3.1",
        "jest": "^23.6.0",
        "lerna": "^3.13.1",
        "prettier": "^1.16.4",
        "prettier-check": "^2.0.0",
        "ts-jest": "^23.10.5",
        "ts-node": "^5.0.1",
        "tslint": "^5.13.1",
        "tslint-config-prettier": "^1.18.0",
        "typescript": "^3.2.2"
    },
    "workspaces": {
        "packages": [
            "packages/*"
        ],
        "nohoist": [
            "**/@serverless-ide/language-server",
            "**/@serverless-ide/language-server/**"
        ]
    },
    "scripts": {
        "update-vscode": "node ./node_modules/vscode/bin/install",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "jest",
        "pre-commit": "yarn lint:types && yarn lint:prettier && yarn lint:ts && yarn test",
        "compile": "lerna run compile",
        "lint:types": "lerna run lint:types",
        "lint:ts": "lerna run lint:ts",
        "lint:ts:fix": "lerna run lint:ts:fix",
        "lint:prettier": "prettier-check packages/**/*.ts"
    },
    "husky": {
        "hooks": {
            "pre-commit": "yarn pre-commit"
        }
    }
}
